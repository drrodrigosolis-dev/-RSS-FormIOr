% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GetRespones.R
\name{GetResponses}
\alias{GetResponses}
\title{Get responses submitted to a 'FormIO' form}
\usage{
GetResponses(
  base_url = "https://submit.digital.gov.bc.ca/app/api/v1",
  form_id = NULL,
  api_key = NULL,
  drafts = FALSE,
  deleted = FALSE,
  content.only = TRUE,
  reenter.credentials = FALSE
)
}
\arguments{
\item{base_url}{Character API base URL. Default:
\code{"https://submit.digital.gov.bc.ca/app/api/v1"}.}

\item{form_id}{Optional form ID. If \code{NULL}, uses cached credentials or
prompts interactively via \code{\link[=AskCredentials]{AskCredentials()}}.}

\item{api_key}{Optional API key. If \code{NULL}, uses cached credentials or
prompts interactively via \code{\link[=AskCredentials]{AskCredentials()}}.}

\item{drafts}{Logical. Include drafts. Default \code{FALSE}.}

\item{deleted}{Logical. Include deleted submissions. Default \code{FALSE}.}

\item{content.only}{Logical or character.
\itemize{
\item \code{TRUE} (default): return parsed submissions
\item \code{FALSE}: return full response list (status, headers, parsed data)
\item \code{"raw"}: return raw JSON text
}}

\item{reenter.credentials}{Logical. Force re-entry of credentials.}
}
\value{
Parsed submissions, full response list, or raw JSON depending on
\code{content.only}.
}
\description{
Downloads response export JSON from a form endpoint and returns parsed
submissions by default.
}
\examples{
\dontrun{
GetResponses(form_id = "your_form_id", api_key = "your_api_key")
}
}
