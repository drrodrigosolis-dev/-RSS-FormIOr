% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GetFormMetadata.R
\name{GetFormMetadata}
\alias{GetFormMetadata}
\title{Get metadata for a 'FormIO' form}
\usage{
GetFormMetadata(
  base_url = "https://submit.digital.gov.bc.ca/app/api/v1",
  form_id = NULL,
  api_key = NULL,
  reenter.credentials = FALSE
)
}
\arguments{
\item{base_url}{Character API base URL. Default:
\code{"https://submit.digital.gov.bc.ca/app/api/v1"}.}

\item{form_id}{Optional form ID. If \code{NULL}, uses cached credentials or
prompts interactively via \code{\link[=AskCredentials]{AskCredentials()}}.}

\item{api_key}{Optional API key. If \code{NULL}, uses cached credentials or
prompts interactively via \code{\link[=AskCredentials]{AskCredentials()}}.}

\item{reenter.credentials}{Logical. Force re-entry of credentials.}
}
\value{
A named list of form metadata.
}
\description{
Fetches form metadata from the API and retries the alternate base URL when
parsing fails (CHEF compatibility behavior).
}
\details{
Returns a hierarchical named list representing the form configuration and
metadata, closely matching the JSON response structure.
}
\examples{
\dontrun{
GetFormMetadata(form_id = "your_form_id", api_key = "your_api_key")
}
}
