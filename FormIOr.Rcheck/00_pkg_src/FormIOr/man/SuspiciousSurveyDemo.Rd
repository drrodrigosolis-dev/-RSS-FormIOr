% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SuspiciousSurveyDemo.R
\docType{data}
\name{SuspiciousSurveyDemo}
\alias{SuspiciousSurveyDemo}
\title{Suspicious Survey Demo Dataset}
\format{
A data frame with 262 rows and 10 variables:
\describe{
\item{submissionId}{Character submission identifier. Some IDs are repeated intentionally.}
\item{created}{POSIXct timestamp in UTC.}
\item{age}{Numeric respondent age.}
\item{satisfaction_score}{Numeric score (0-100).}
\item{region}{Categorical region (\code{North}, \code{South}, \code{East}, \code{West}).}
\item{program}{Categorical program (\code{A}, \code{B}, \code{C}).}
\item{consent}{Categorical consent indicator (\code{Yes}, \code{No}).}
\item{comment}{Short free-text category.}
\item{phase_expected}{Expected phase label (\code{pre}, \code{post}).}
\item{mostly_missing}{Mostly missing text field for completeness-threshold tests.}
}
}
\source{
Synthetic data generated in \code{data-raw/make_suspicious_survey_demo.R}.
}
\usage{
data("SuspiciousSurveyDemo")
}
\description{
A synthetic survey-response dataset designed to test
\code{\link[=FlagSuspiciousSubmissions]{FlagSuspiciousSubmissions()}}.
}
\details{
It includes:
\itemize{
\item A pre-change (anonymous) phase and post-change (identity-verified) phase.
\item Intentional distribution shifts between phases (numeric and categorical).
\item Repeated submission IDs with close timestamps.
\item Repeated answer fingerprints with different submission IDs.
\item A low-completeness field (\code{mostly_missing}) for completeness-filter testing.
}
}
\examples{
data("SuspiciousSurveyDemo")
str(SuspiciousSurveyDemo)

out <- FlagSuspiciousSubmissions(
  SuspiciousSurveyDemo,
  id_col = "submissionId",
  time_col = "created",
  cutoff_time = "2026-01-06 00:00:00",
  group_action = "split_only",
  quiet = TRUE
)
out$comparability$non_comparable
}
\keyword{datasets}
