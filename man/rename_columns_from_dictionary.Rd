% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renameCols.R
\name{rename_columns_from_dictionary}
\alias{rename_columns_from_dictionary}
\title{Rename columns from a flattened FormIO dataset}
\usage{
rename_columns_from_dictionary(
  x,
  NamesDF = TRUE,
  renameDF = TRUE,
  rename_map = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{List from \code{\link[=flatten_submission_records]{flatten_submission_records()}} containing \code{FlatResponses} and
\code{ColumnNames}.}

\item{NamesDF}{Logical. Include the rename table in the output. Default \code{TRUE}.}

\item{renameDF}{Logical. Include the renamed flat object in the output.
Default \code{TRUE}.}

\item{rename_map}{Optional data frame with \code{OldNames} and \code{NewNames} columns
for non-interactive renaming.}

\item{quiet}{Logical. If \code{TRUE}, suppresses preview output.}
}
\value{
A named list with:
\itemize{
\item \code{renamedDF}: rename mapping table (\code{Number}, \code{OldNames}, \code{NewNames})
\item \code{flat}: updated flattened object with renamed \code{FlatResponses}
}
}
\description{
Renames columns in a list returned by \code{\link[=flatten_submission_records]{flatten_submission_records()}}.
You can run it interactively (prompt for each column) or non-interactively
with \code{rename_map}.
}
\examples{
\dontrun{
# Interactive
rename_columns_from_dictionary(flat)

# Non-interactive map
map_df <- data.frame(
  OldNames = c("submissionId", "age"),
  NewNames = c("submission_id", "age_years"),
  stringsAsFactors = FALSE
)
rename_columns_from_dictionary(flat, rename_map = map_df, quiet = TRUE)
}
}
