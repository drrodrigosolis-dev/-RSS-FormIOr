% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReportingExport.R
\name{export_results_to_excel}
\alias{export_results_to_excel}
\title{Export data to Excel (or CSV if needed)}
\usage{
export_results_to_excel(
  data,
  path = NULL,
  sheet = "Data",
  overwrite = FALSE,
  include_row_names = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame, list of data.frames, or list containing
\code{FlatResponses} or \code{submission_data}.}

\item{path}{File path for the Excel output. Required. If you use a \code{.csv} or
\code{.tsv} extension, delimited files will be written.}

\item{sheet}{Sheet name to use when exporting a single data.frame.}

\item{overwrite}{Logical. If \code{FALSE} (default), stop if the file exists.}

\item{include_row_names}{Logical. Include row names in the export.}

\item{quiet}{Logical. If \code{FALSE}, prints a short message.}
}
\value{
A list with:
\describe{
\item{path}{Output file path(s).}
\item{format}{\code{"xlsx"}, \code{"csv"}, or \code{"tsv"}.}
\item{sheets}{Sheet names used.}
\item{rows}{Row counts for each sheet.}
\item{cols}{Column counts for each sheet.}
}
}
\description{
This is a simple, user-friendly export helper for FormIOr outputs.
It accepts a data.frame, a list of data.frames (multiple sheets),
or a list returned by \code{\link[=fetch_form_responses]{fetch_form_responses()}} / \code{\link[=flatten_submission_records]{flatten_submission_records()}}.
If audit logging is active (see \code{\link[=start_audit_log]{start_audit_log()}}), this action is recorded.
}
\details{
If an Excel writer is available (\code{writexl} or \code{openxlsx}), it writes \code{.xlsx}.
If not, it falls back to CSV files and prints a clear message.
}
\examples{
df <- data.frame(name = c("A", "B"), score = c(1, 2))
\dontrun{
  export_results_to_excel(df, tempfile(fileext = ".xlsx"), overwrite = TRUE)
}
}
