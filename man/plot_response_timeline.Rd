% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AnalysisHelpers.R
\name{plot_response_timeline}
\alias{plot_response_timeline}
\title{Plot a response timeline}
\usage{
plot_response_timeline(
  x,
  date_col = NULL,
  interval = c("day", "week", "month", "hour"),
  tz = "UTC",
  start = NULL,
  end = NULL,
  start_date = NULL,
  end_date = NULL,
  include_empty = TRUE,
  main = NULL,
  xlab = NULL,
  ylab = "Responses",
  col = "steelblue",
  lwd = 2,
  type = "l",
  plot = TRUE,
  ...
)
}
\arguments{
\item{x}{A data frame of responses, or a list from \code{\link[=flatten_submission_records]{flatten_submission_records()}}.}

\item{date_col}{Column name or number for the date/time field. If \code{NULL},
tries to guess a reasonable timestamp column.}

\item{interval}{One of \code{"day"} (default), \code{"week"}, \code{"month"}, or \code{"hour"}.}

\item{tz}{Time zone to use for parsing dates (default \code{"UTC"}).}

\item{start}{Optional start date/time to filter the range. Accepts a \code{Date},
\code{POSIXct}, or an ISO-8601 string (e.g., \code{"2024-03-01"} or
\code{"2024-03-01 14:30:00"}).}

\item{end}{Optional end date/time to filter the range. Accepts a \code{Date},
\code{POSIXct}, or an ISO-8601 string (e.g., \code{"2024-03-31"} or
\code{"2024-03-31 23:59:59"}).}

\item{start_date}{Alias for \code{start} (useful for readability). Same formats
as \code{start}.}

\item{end_date}{Alias for \code{end} (useful for readability). Same formats
as \code{end}.}

\item{include_empty}{Logical. If \code{TRUE}, fill missing periods with zeroes.}

\item{main}{Optional plot title.}

\item{xlab}{Optional x-axis label.}

\item{ylab}{Optional y-axis label.}

\item{col}{Line color.}

\item{lwd}{Line width.}

\item{type}{Plot type passed to \code{\link[=plot]{plot()}}. Default "l".}

\item{plot}{Logical. If \code{FALSE}, return timeline data without plotting.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
Invisibly returns the output of \code{\link[=summarize_response_timeline]{summarize_response_timeline()}}.
}
\description{
Convenience wrapper around \code{\link[=summarize_response_timeline]{summarize_response_timeline()}} that draws a simple line
chart using base graphics.
If audit logging is active (see \code{\link[=start_audit_log]{start_audit_log()}}), this action is recorded.
}
\examples{
\dontrun{
plot_response_timeline(flat, date_col = "created", interval = "month")
}
}
